spring.application.name=api-agendamento

# --- CACHE E REDIS ---
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.cache.redis.time-to-live=600000
spring.cache.type=redis

# --- BANCO DE DADOS (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5433/stylo_db
spring.datasource.username=admin
spring.datasource.password=password123
spring.datasource.driver-class-name=org.postgresql.Driver

# --- HIBERNATE / JPA ---
# ATENÇÃO: Em produção, o DDL-AUTO deve ser 'validate' ou 'none' para não dropar tabelas sem querer.
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- INTEGRAÇÕES (Resend, Google, Stripe) ---
resend.api.key=${RESEND_API_KEY:chave_local_vazia}
resend.from.email=${RESEND_FROM_EMAIL:Stylo <notificacoes@seudominio.com.br>}

google.calendar.client-id=${GOOGLE_CLIENT_ID:chave_local_vazia}
google.calendar.client-secret=${GOOGLE_CLIENT_SECRET:chave_local_vazia}

stripe.api.key=${STRIPE_API_KEY:sk_test_local}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_local}

# --- CONFIGURAÇÕES DO SAAS STYLO ---
stylo.api-url=${API_URL:http://localhost:8080}
stylo.frontend-url=${FRONTEND_URL:http://localhost:5173}
stylo.webhook-secret=${INTERNAL_WEBHOOK_SECRET:S3cr3t_Stylo_Sup3r_S3cur3_T0k3n_2026}
stylo.jobs.reminder-interval=${REMINDER_INTERVAL:60000}